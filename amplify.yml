version: 1
backend:
  phases:
    build:
      commands:
        - echo "No backend build required"
frontend:
  phases:
    preBuild:
      commands:
        - pip install -r requirements.txt
    build:
      commands:
        - echo "Building Friday BMS Flask application"
    postBuild:
      commands:
        - echo "Build completed successfully"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
